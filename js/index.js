

$(window).on('load', function() {
    $(window).scrollTop(0);
});

//  MENU COLLAPSE ----------------------------------------------------

  $("li").on("click", function () {
    $('.navbar-collapse').collapse('hide');
  });

  